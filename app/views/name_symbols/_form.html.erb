<%= form_with(model: name_symbol) do |form| %>
  <% if name_symbol.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(name_symbol.errors.count, "error") %> prohibited this name_symbol from being saved:</h2>

      <ul>
        <% name_symbol.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name_of_element, style: "display: block" %>
    <%= form.text_field :name_of_element %>
  </div>

  <div>
    <%= form.label :symbo_of_element, style: "display: block" %>
    <%= form.text_field :symbo_of_element %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
